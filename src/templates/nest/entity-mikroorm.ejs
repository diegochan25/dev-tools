import { Entity, PrimaryKey } from "@mikro-orm/core";
import { v4 as uuidv4 } from "uuid";

@Entity({ tableName: "<%- names.snake %>" })
export class <%- names.pascal %> {
    @PrimaryKey()
    id: string = uuidv4();
}
