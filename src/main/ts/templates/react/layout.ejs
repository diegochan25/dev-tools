<%if (useStyles) {%>import styles from "./<%- names.kebab %>.module.css";

<%}%>export default function <%- names.pascal %>Layout()<% if (useTypescript) {%>: JSX.Element<%}%> {
    return (
        <>
            
        </>
    );
}