<%- rules.javascript.moduleImporter(["Module"], "@nestjs/common") %><% if (useController) { %>
<%- rules.javascript.moduleImporter([`${names.pascal}Controller`], `./${names.kebab}.controller`) %><%}%><% if (useService) {%>
<%- rules.javascript.moduleImporter([`${names.pascal}Service`], `./${names.kebab}.service`) %><%}%>

@Module({
    imports: [],
    controllers: [<% if (useController) {%><%- names.pascal %>Controller<%}%>],
    providers: [<% if (useService) {%><%- names.pascal %>Service<%}%>]
})
export class <%- names.pascal %>Module { }