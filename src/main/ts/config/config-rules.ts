export enum JavaScriptQuoteRule {
    Single = "single",
    Double = "double"
}

export enum JavaScriptSemiColonRule {
    Use = "use",
    Omit = "omit"
}

export enum JavaScriptIndentationRule {
    Tab = "tab",
    Space = "space"
}

export enum JavaScriptObjectSpacingRule {
    Space = "space",
    Tight = "tight",
}

export enum JavaScriptFileNameCasingRule {
    Pascal = "pascal",
    Kebab = "kebab"
}

export enum JavaScriptRuntimeRule {
    Node = "node",
    Deno = "deno",
    Bun = "bun"
}

export enum JavaScriptPackageManagerRule {
    Npm = "npm",
    Yarn = "yarn",
    Pnpm = "pnpm",
    Bun = "bun"
}

export enum JavaScriptModuleSystemRule {
    CommonJS = "commonjs",
    ES6 = "es6"
}

export enum NestORMRule {
    TypeOrm = "typeorm",
    Sequelize = "sequelize",
    Prisma = "prisma",
    MikroOrm = "mikroorm"
}

export enum NestDbSyntaxRule {
    SQL = "sql",
    MongoDB = "mongodb"
}

export enum ReactLanguageRule {
    JS = "js",
    TS = "ts",
    JSX = "jsx",
    TSX = "tsx"
}

export enum ReactElementReturnTypeRule {
    ReactNode = "react-node",
    JSXElement = "jsx-element"
}